# attendence_predictor
Attendance Predictor is a web application built using Python, Flask, HTML, and CSS. It calculates a student’s current attendance percentage and predicts how many classes are required to reach the minimum threshold. The project demonstrates basic web development, logical implementation, and integration between frontend and backend.read
